export interface ImageObject {
  imageId: string;
  canvas: string;
}

export type Canvas = string; // or whatever shape it’s supposed to have
export interface ImageObject {
  imageId: string;
  canvas: string;
}